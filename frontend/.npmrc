# ═══════════════════════════════════════════════════════════════════════════════
# nself-chat pnpm Configuration
# ═══════════════════════════════════════════════════════════════════════════════

# Hoist all dependencies for compatibility with packages expecting flat node_modules
shamefully-hoist=true

# Don't fail on peer dependency issues - auto-install peers instead
strict-peer-dependencies=false
auto-install-peers=true

# Use the latest lockfile format
lockfile-format=v9

# Store location (use default global store for efficiency)
# store-dir=~/.pnpm-store

# Prefer frozen lockfile in CI for reproducible builds
# Set PNPM_FROZEN_LOCKFILE=true in CI environment

# Improve security by not running lifecycle scripts from dependencies
# ignore-scripts=false

# Public packages hoisting (for monorepo compatibility)
public-hoist-pattern[]=*types*
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*prettier*

# Node.js version enforcement (optional - uncomment if needed)
# use-node-version=20.11.0

# Registry configuration
# registry=https://registry.npmjs.org/
